pandas>=3.0
numpy>=2.4
xgboost>=3.4
scikit-learn>=1.9
scipy>=1.18
langgraph>=1.2
shap>=0.52
rank_bm25>=0.2
python-dotenv>=1.2
requests>=2.34
mcp==2.0.0
streamlit>=1.61
matplotlib>=3.10
pytest>=9.1
mlflow-skinny==3.15.1  # NOT `mlflow` -- full mlflow==3.15.1 requires pandas<3, which conflicts with
# this project's pandas>=3.0 and silently downgrades an already-installed pandas 3.0.5 (breaks reading
# any .pkl written under 3.0's StringDtype format -- discovered live, not theoretical). mlflow-skinny has
# no pandas dependency at all and includes the full tracking client (start_run/log_params/log_metrics/
# log_artifact) -- everything experiments/tracking.py uses. It does NOT include the `mlflow ui` server;
# viewing results locally needs a one-off `pip install mlflow` in a SEPARATE/disposable environment.
sentence-transformers==5.7.0
# experiments/retrieval_stack.py pins these two HF models by revision (verified
# live against huggingface.co/api/models/... before pinning, not from memory):
#   bi-encoder:    BAAI/bge-small-en-v1.5              rev 5c38ec7c405ec4b44b94cc5a9bb96e735b38267a
#   cross-encoder: cross-encoder/ms-marco-MiniLM-L6-v2 rev 233902d25c440f23af6f7d6e94d2946bac0bee0a
# (downloaded from the HF Hub on first run, not installed via pip)
